---
description: Writes a standing /goal prompt and the plan file a loop re-reads, built to terminate rather than spin.
---

Invoke the `nzs-marathon` skill and follow it exactly. Arguments: $ARGUMENTS
